name: sonorie
description: Sonorie Flutter real migration.
publish_to: "none"
version: 0.4.2+46
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  just_audio: ^0.9.42
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
